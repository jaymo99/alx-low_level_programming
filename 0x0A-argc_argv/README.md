# Directory containing task files for argc and argv in C

- We can pass arguments to the main function in C

- argc: argument count
- argv: argument vector. an array of strings that hold the arguments.
